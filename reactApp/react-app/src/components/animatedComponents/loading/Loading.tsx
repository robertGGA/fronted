import {CSSTransition} from 'react-transition-group';
import "./loadingStyles.sass";
import load from "./../../../assets/Indeterminate_Spinner_.svg"
import {Component} from "react";

const Loader = () => {
    // let q = new CSSTransition("fdf");
    //     return (
    //         <CSSTransition timeout={3000} className="loader">
    //             <img src={load} className={}/>
    //         </CSSTransition>
    //     )
}

// export default Loader();